APP_PORT=8080 # Canonical app port
PORT=8080 # PV_D-A-CH webui port
DB_BACKEND=postgres # mysql or postgres
DB_HOST=127.0.0.1 # DB host
DB_PORT=5432 # DB port
DB_USER=citadel_pvdach # DB user
DB_PASSWORD= # DB password
DB_NAME=citadel_pvdach # DB name
OPENAI_API_BASE=http://127.0.0.1:4000/v1 # OpenAI-compatible endpoint
OPENAI_API_KEY= # Optional bearer token
DEFAULT_MODEL=openai/gpt-5.4 # Unified model default
